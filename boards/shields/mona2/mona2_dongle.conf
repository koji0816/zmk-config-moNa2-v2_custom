# Dongle config
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y

# Pointing config (右手のトラックボールデータを受信するために必要)
CONFIG_ZMK_POINTING=y

# Display config
CONFIG_ZMK_DISPLAY=y
CONFIG_XIAORD_BG_1=y

# Bluetooth config (接続の安定化)
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
